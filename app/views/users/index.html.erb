<h1>Users</h1>

<% if @users %>
    <ul>
      <% @users.each do |user| %>
          <li><%= link_to(user.title, user_path(:id => user.obj_id)) %> <%= " - #{user.inspect}" if @params[:inspect]%></li>
      <% end %>
    </ul>
<% else %>
    <div>Forbidden</div>
<% end %>
